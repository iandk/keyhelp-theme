{% extends 'base/intern.twig' %}



{% block page_scripts %}
    <script>
        var directoryBrowserPurpose = 'ftp';
    </script>
{% endblock %}



{% block content %}

    {% include 'includes/card_start.twig' %}

        <form action="?page=user_ftp&action=edit" method="post" autocomplete="off">
            <input type="hidden" name="submit" value="1">
            <input type="hidden" name="id" value="{{ _id }}">

            {{ forms.input_wrapped({
                'name': 'username',
                'type': 'text',
                'label': username,
                'value': _username,
                'is_disabled': true,
            }) }}

            {{ forms.input_wrapped({
                'name': 'description',
                'type': 'text',
                'label': description,
                'value': _description,
            }) }}

            {% include 'includes/form_browse_directory.twig' with {
                '_label': home_directory,
                '_help': home_directory_help,
                '_value': _path
            }%}

            {% include 'includes/form_password.twig' with { '_leave_empty': true } %}

            {% include 'includes/cta.twig' with { '_back': { 'target': 'user_ftp' }, '_confirm': true } %}
        </form>

    {% include 'includes/card_end.twig' %}

{% endblock %}